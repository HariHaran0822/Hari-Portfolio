import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.scss";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Hari's Portfolio",
  description:
    "Explore a showcase of my diverse portfolio, featuring innovative projects in web development, design, and more. Witness my passion for creating impactful digital experiences and solving real-world problems",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
